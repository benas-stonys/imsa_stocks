{
  "name": "classroom-trading-simulator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.35.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --dir=. --functions=netlify/functions"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "jsdom": "^30.1.1"
  }
}
